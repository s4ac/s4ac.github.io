function setup() {
	createCanvas(windowWidth, windowHeight);
}

function draw() {

}
// responsive design
function windowResized(){
	resizeCanvas(windowWidth, windowHeight);
	// here you should add code to scale the dimension of your elements in the canvas
}